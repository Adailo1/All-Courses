//Crie uma função que receba uma array e imprima no console o número do índice e o elemento.

const listaAmigos = ['João', 'Pedro', 'Ed', 'Bruno']
for (let i = 0; i < listaAmigos.length ; i++){
    console.log(i, listaAmigos[i])
}